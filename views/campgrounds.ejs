<%- include("partials/header") %>

<h1>This is the camp grounds page</h1>

<% campgrounds.forEach( campground => { %>
    <div>
        <h4><%= campground.name %></h4>
        <img style="width: 300px; height: 150px;" src="<%= campground.image %>">
    </div>

<% }); %>

<%- include("partials/footer") %>